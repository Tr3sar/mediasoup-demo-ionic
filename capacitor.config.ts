import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mediasoup-demo-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
